<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Task Details</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body class="bg-gradient-to-br from-slate-900 via-zinc-900 to-black text-white min-h-screen">

    <div class="max-w-4xl mx-auto px-6 py-12">

        <!-- Back Button -->
        <button onclick="window.history.back()"
            class="inline-flex items-center gap-2 px-4 py-2 mb-8 rounded-lg bg-white/5 border border-white/10 hover:bg-white/10 transition duration-300 text-blue-400 hover:text-blue-300 cursor-pointer">
            ← Go Back
        </button>

        <!-- Task Card -->
        <div
            class="bg-white/5 backdrop-blur-lg border border-white/10 rounded-2xl shadow-2xl p-10 hover:shadow-blue-500/10 transition duration-500">

            <!-- Title -->
            <h1 class="text-3xl md:text-4xl font-bold leading-tight mb-6">
                <%= filename?.split("_").join(" ").slice(0, -4) %>
            </h1>

            <!-- Content -->
            <p class="text-lg text-gray-300 leading-relaxed whitespace-pre-line mb-10">
                <%= data %>
            </p>

            <!-- Action Buttons -->
            <div class="flex flex-col sm:flex-row gap-4 justify-end">
                <!-- Edit Button -->
                <a href="/edit/<%= filename %>"
                    class="px-6 py-3 rounded-xl bg-gradient-to-r from-blue-600 to-indigo-600 hover:from-blue-500 hover:to-indigo-500 font-semibold shadow-lg hover:shadow-blue-500/30 transition duration-300 transform hover:-translate-y-1 text-center">
                    Edit
                </a>

                <!-- Delete Button -->
                <a href="/delete/<%= filename %>"
                    class="px-6 py-3 rounded-xl bg-gradient-to-r from-red-600 to-rose-500 hover:from-red-500 hover:to-rose-400 font-semibold shadow-lg hover:shadow-red-500/30 transition duration-300 transform hover:-translate-y-1 text-center">
                    Delete
                </a>
            </div>

        </div>

    </div>

</body>

</html>