<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Task</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body class="bg-gradient-to-br from-slate-900 via-zinc-900 to-black text-white min-h-screen">

    <div class="max-w-4xl mx-auto px-6 py-12">

        <!-- Back Button -->
        <button onclick="window.history.back()" 
           class="inline-flex items-center gap-2 px-4 py-2 mb-10 rounded-lg bg-white/5 border border-white/10 hover:bg-white/10 transition duration-300 text-blue-400 hover:text-blue-300 cursor-pointer">
            ← Go Back
        </button>

        <!-- Edit Card -->
        <div class="bg-white/5 backdrop-blur-lg border border-white/10 rounded-2xl shadow-2xl p-10 hover:shadow-yellow-500/10 transition duration-500">

            <h1 class="text-3xl md:text-4xl font-bold mb-2">
                Edit Task
            </h1>
            <p class="text-gray-400 mb-8">
                Update your task details below
            </p>

            <form action="/update/<%= fileName %>" method="post" class="space-y-6">

                <!-- Title Input -->
                <div>
                    <label class="block text-sm text-gray-400 mb-2">Title</label>
                    <input 
                        type="text"
                        name="title"
                        required
                        value="<%= fileName.split('_').join(' ').slice(0, -4) %>"
                        class="w-full px-5 py-3 rounded-xl bg-zinc-800/80 border border-zinc-700 focus:border-yellow-500 focus:ring-2 focus:ring-yellow-500/40 outline-none transition"
                    />
                </div>

                <!-- Details -->
                <div>
                    <label class="block text-sm text-gray-400 mb-2">Details</label>
                    <textarea
                        name="details"
                        required
                        class="w-full px-5 py-3 rounded-xl bg-zinc-800/80 border border-zinc-700 focus:border-yellow-500 focus:ring-2 focus:ring-yellow-500/40 outline-none resize-none h-40 transition"
                    ><%= fileData %></textarea>
                </div>

                <!-- Submit Button -->
                <button 
                    type="submit"
                    class="w-full py-3 rounded-xl bg-gradient-to-r from-yellow-500 to-amber-500 hover:from-yellow-400 hover:to-amber-400 font-semibold tracking-wide shadow-lg hover:shadow-yellow-500/30 transition duration-300 transform hover:-translate-y-1 cursor-pointer"
                >
                    Update Task
                </button>

            </form>

        </div>

    </div>

</body>
</html>