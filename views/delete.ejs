<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Delete Task</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body class="bg-gradient-to-br from-slate-900 via-zinc-900 to-black text-white min-h-screen">

    <div class="max-w-3xl mx-auto px-6 py-16 flex flex-col items-center">

        <!-- Warning Card -->
        <div class="bg-white/5 backdrop-blur-lg border border-white/10 rounded-2xl shadow-2xl p-10 text-center w-full hover:shadow-red-500/10 transition duration-500">

            <h1 class="text-3xl md:text-4xl font-bold text-red-500 mb-4">
                Delete Task
            </h1>

            <p class="text-gray-300 mb-8 text-lg">
                Are you sure you want to delete this task? <br/>
                This action <span class="font-semibold text-red-400">cannot be undone</span>.
            </p>

            <p class="text-xl font-semibold mb-6">
                "<%= fileName?.split('_').join(' ').slice(0, -4) %>"
            </p>

            <div class="flex flex-col sm:flex-row gap-4 justify-center">

                <!-- Cancel Button -->
                <button onclick="window.history.back()" 
                   class="px-6 py-3 rounded-xl bg-white/5 border border-white/10 hover:bg-white/10 text-gray-300 hover:text-white transition duration-300 cursor-pointer">
                    Cancel
                </button>

                <!-- Delete Form -->
                <form action="/delete/<%= fileName %>" method="post">
                    <button type="submit"
                        class="px-6 py-3 rounded-xl bg-gradient-to-r from-red-600 to-rose-500 hover:from-red-500 hover:to-rose-400 font-semibold shadow-lg hover:shadow-red-500/30 transition duration-300 transform hover:-translate-y-1 cursor-pointer">
                        Delete
                    </button>
                </form>

            </div>

        </div>

    </div>

</body>
</html>